<?php    # @uthor armando_rdz, at 11/04/20    require '../db/Database.php';    $db = new Database();    $con = $db -> connect();    $sqlConsolas = "SELECT con.id, con.numero, pl.id, pl.nombre, pl.cobro, pl.fecha_lanzamiento                    FROM consolas con                     LEFT JOIN plataformas pl on con.id_plataforma = pl.id                    ORDER BY con.numero ASC";    $resultConsoals = mysqli_query($con, $sqlConsolas);    while ($row = mysqli_fetch_row($resultConsoals)){    ?>    <?php    }    ?>